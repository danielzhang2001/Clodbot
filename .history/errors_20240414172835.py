def invalid_replay_link_error(link):
    return f"**{link}** is an invalid replay link."


def invalid_sheets_link_error(link):
    return f"**{link}** is an invalid Google Sheets link."


def unexpected_error(error_message):
    return f"Unexpected error occurred: {error_message}"
